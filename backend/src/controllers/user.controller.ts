import { Request, Response } from "express";

const signUp = async (req: Request, res: Response) => {
  try {
    const {success }
    
  } catch (error) {
    console.error()
  }
};
export { signUp };
